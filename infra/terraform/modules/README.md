# Terraform modules

Reusable Terraform modules should live here.

Rules:

- Keep modules small and purpose-specific.
- Document inputs, outputs, and default cost behavior.
- Avoid hardcoded environment names or secrets.
- Add examples when module behavior is non-obvious.
